[package]
name = "surface-dial-rs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
hidapi = { version = "1.2.3", default-features = false, features = ["linux-shared-hidraw"] }
flume = "0.10.12"